# دليل التثبيت الكامل - خطوة بخطوة

> اتبع الخطوات بالترتيب. كل قسم بياخد 5-15 دقيقة.

---

## القسم الأول: تثبيت Python (5 دقائق)

### 1. حمّل Python
- روح: https://www.python.org/downloads/
- اضغط الزرار الأصفر **"Download Python 3.x.x"** (لازم 3.10 أو أحدث)

### 2. ثبّت Python (مهم جداً!)
- شغّل ملف `python-3.x.x-amd64.exe` اللي نزّلته
- ✅ **علّم على المربع: "Add Python to PATH"** (تحت في الشاشة الأولى) - **ده مهم جداً**
- ✅ علّم على: "Install launcher for all users" (اختياري)
- اضغط **"Install Now"**
- استنى ~3 دقايق
- في النهاية: اضغط **"Disable path length limit"** لو ظهرت

### 3. تأكد إن Python اتثبت
افتح **PowerShell جديد** (مهم: جديد بعد التثبيت) واكتب:
```powershell
python --version
pip --version
```
المفروض يطلع:
```
Python 3.12.x
pip 24.x.x from ...
```

❌ لو طلعلك خطأ "command not found": **اقفل وافتح PowerShell جديد**.

---

## القسم الثاني: تثبيت ffmpeg (5 دقائق)

### 1. حمّل ffmpeg
- روح: https://www.gyan.dev/ffmpeg/builds/
- في قسم **"release builds"** اضغط: **`ffmpeg-release-essentials.zip`**
- (الحجم ~80MB)

### 2. فك الضغط في مكان دائم
- افتح الملف المضغوط
- انسخ الفولدر اللي جواه (اسمه زي `ffmpeg-7.x-essentials_build`)
- الصقه في `C:\` بعد ما تغير اسمه لـ **`ffmpeg`**
- المفروض يبقى المسار: `C:\ffmpeg\bin\ffmpeg.exe`

### 3. ضيف ffmpeg لـ PATH
- اضغط **Windows + R** → اكتب `sysdm.cpl` → Enter
- روح تاب **"Advanced"** → اضغط **"Environment Variables..."**
- في القسم العلوي (User variables): دور على **`Path`** → دبل كليك
- اضغط **"New"** → اكتب: `C:\ffmpeg\bin`
- **OK** → **OK** → **OK**

### 4. تأكد إن ffmpeg شغال
**اقفل PowerShell وافتح واحد جديد** واكتب:
```powershell
ffmpeg -version
```
المفروض يطلع: `ffmpeg version 7.x ...`

---

## القسم الثالث: تثبيت مكتبات المشروع (3 دقائق)

في PowerShell، روح للمشروع وشغّل setup:
```powershell
cd "C:\Users\aldaa\OneDrive\Desktop\youtube-auto-uploader"
.\setup.bat
```

ده هيعمل:
- بيئة افتراضية (venv)
- تثبيت كل المكتبات (Whisper, Gemini, YouTube API, إلخ)
- ينسخ config.json من template

⏱️ هياخد 3-5 دقايق (faster-whisper كبير شوية)

✅ لما يخلص هيقولك "تم التثبيت بنجاح".

---

## القسم الرابع: YouTube OAuth (10-15 دقيقة) - الأهم

ده اللي بيخلي الأداة ترفع للقناة بتاعتك تلقائياً.

### 1. روح Google Cloud Console
- افتح: https://console.cloud.google.com/
- سجل دخول بـ Gmail اللي عليه قناتك

### 2. اعمل Project جديد
- فوق على الشمال اضغط على المنيو "Select a project"
- اضغط **"New Project"**
- Project name: **`Naqal Khair Uploader`**
- اضغط **"Create"**
- استنى لحد ما يخلصه (~10 ثواني)
- اختار الـ project من الـ dropdown فوق

### 3. فعّل YouTube Data API v3
- في الـ search bar فوق اكتب: **`YouTube Data API v3`**
- اضغط على النتيجة
- اضغط الزرار الأزرق **"Enable"**
- استنى لحد ما يفعّله

### 4. اعمل OAuth Consent Screen
- في القائمة الجانبية: **APIs & Services** → **OAuth consent screen**
- اختار **"External"** → **Create**
- املأ:
  - App name: **`Naqal Khair Uploader`**
  - User support email: إيميلك
  - Developer contact: إيميلك
  - الباقي خليه فاضي
- اضغط **Save and Continue**
- في **Scopes**: اضغط Save and Continue (مش هنضيف حاجة)
- في **Test users**:
  - اضغط **"Add Users"**
  - اكتب إيميل القناة بتاعتك (الـ Gmail)
  - **Save**
- Save and Continue → Back to Dashboard

### 5. اعمل OAuth Client ID
- في القائمة الجانبية: **APIs & Services** → **Credentials**
- فوق اضغط **"+ Create Credentials"** → **"OAuth client ID"**
- Application type: اختار **"Desktop app"**
- Name: **`Naqal Khair Desktop`**
- اضغط **"Create"**

### 6. حمّل ملف client_secret.json
- هيظهرلك popup فيه Client ID و Client secret
- اضغط **"Download JSON"** (الزرار اليمين)
- هينزل ملف اسمه طويل زي `client_secret_xxxxxxxx.json`

### 7. حط الملف في المشروع
- غيّر اسم الملف لـ **`client_secret.json`** بالظبط
- انقله إلى:
  ```
  C:\Users\aldaa\OneDrive\Desktop\youtube-auto-uploader\credentials\client_secret.json
  ```

✅ خلصت OAuth!

---

## القسم الخامس: التجربة الأولى (10-30 دقيقة)

### dry-run على فيديو واحد (بدون رفع)

في PowerShell:
```powershell
cd "C:\Users\aldaa\OneDrive\Desktop\youtube-auto-uploader"
.\run.bat upload "E:\فضيلة الشيخ أبي حفص\مرئيات\شرح الرسالة\ترجمةالشافعي\1.wmv" --dry-run
```

⏱️ هياخد:
- بدون GPU: 30-60 دقيقة (Whisper بطيء على CPU)
- بـ GPU NVIDIA: 5-10 دقايق

اللي هيحصل بالتفصيل:
1. **تحميل موديل Whisper** (أول مرة بس - ~1.5 جيجا)
2. **تفريغ الصوت** → SRT في `output/srt/1.srt`
3. **Gemini يحلل النص** → عنوان + وصف + chapters + 3 shorts
4. **عمل thumbnail** → في `output/thumbnails/1.jpg`
5. **قص 3 shorts** → في `output/shorts/1/`

### راجع المخرجات
- افتح `output/metadata/1.json` وشوف العنوان والوصف
- افتح `output/thumbnails/1.jpg` وشوف الـ thumbnail (مع اللوجو)
- شغل واحد من الـ shorts (عمودي 9:16 + ترجمة)

### لو كل حاجة تمام، شيل --dry-run وشغل تاني للرفع الفعلي:
```powershell
.\run.bat upload "E:\فضيلة الشيخ أبي حفص\مرئيات\شرح الرسالة\ترجمةالشافعي\1.wmv"
```

أول مرة هيفتحلك المتصفح عشان تسجل دخول لـ Gmail بتاع القناة وتوافق على الصلاحيات. بعد كده مش هيسأل تاني.

---

## استكشاف الأخطاء

| المشكلة | الحل |
|---|---|
| `'python' is not recognized` | افتح PowerShell جديد (لازم بعد التثبيت) |
| `'ffmpeg' is not recognized` | تأكد من PATH وافتح PowerShell جديد |
| `setup.bat فشل` | شغّل PowerShell كـ Administrator |
| Whisper بطيء جداً | غير `model_size` في config.json من `"medium"` لـ `"small"` أو `"base"` |
| `quota exceeded` | استنى يوم - الـ rotation هيشتغل تلقائياً مع 3 مفاتيح |
| Gemini error | تأكد إن المفاتيح في config.json صحيحة |

---

## الـ checklist النهائي

- [ ] Python مثبت ومتاح (`python --version`)
- [ ] ffmpeg مثبت ومتاح (`ffmpeg -version`)
- [ ] `setup.bat` اشتغل بنجاح
- [ ] `config.json` فيه Gemini keys (موجودين فعلاً)
- [ ] `assets/logo.png` موجود (موجود فعلاً)
- [ ] `credentials/client_secret.json` موجود ← **ده اللي محتاج تعمله**
- [ ] جربت dry-run على فيديو
- [ ] شغلت رفع حقيقي

---

أي خطوة فيها مشكلة، قولي على الجزء بالظبط وأنا أساعدك.
